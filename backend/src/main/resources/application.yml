spring:
  application:
    name: RelayBloom
  
  datasource:
    # Production Database (Supabase PostgreSQL)
    url: jdbc:postgresql://db.kqbriwuqfxnryawqjzuh.supabase.co:5432/postgres
    driverClassName: org.postgresql.Driver
    username: postgres
    password: "kubase:rlaskagnsvosalxld"
  
  sql:
    init:
      mode: never # 실제 DB에서는 로컬 테스트용 샘플 데이터를 매번 넣지 않도록 비활성화
      
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect

  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8080
