spring:
  application:
    name: RelayBloom

  datasource:
    url: jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres?prepareThreshold=0
    driverClassName: org.postgresql.Driver
    username: "postgres.kqbriwuqfxnryawqjzuh"
    password: "rlaskagnsvosalxld"
    hikari:
      minimum-idle: 2
      maximum-pool-size: 5
      idle-timeout: 30000
      max-lifetime: 600000
      connection-timeout: 20000
      keepalive-time: 60000
      validation-timeout: 5000

  sql:
    init:
      mode: never

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          batch_size: 20
        order_inserts: true
        order_updates: true

server:
  port: ${PORT:8080}

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
